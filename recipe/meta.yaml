{% set version = "3.0.5" %}

package:
  name: pytest-spec
  version: {{ version }}

source:
  fn: pytest-spec-{{ version }}.tar.gz
  url: https://github.com/pchomik/pytest-spec/archive/{{ version }}.tar.gz
  sha256: da03e9b104f7617e4c4c84adfbd6993dbdc9b5fb113b056fc54314de1f32781f

build:
  script: python setup.py install --single-version-externally-managed --record record.txt
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools
    - pytest
    - mock >=1.0.1

test:
  source_files:
    - test
  commands:
    - py.test test

about:
  home: https://github.com/pchomik/pytest-spec
  license: GPL-2.0
  license_family: GPL2
  license_file: LICENSE.txt
  summary: pytest plugin to display test execution output like a SPECIFICATION

  dev_url: https://github.com/pchomik/pytest-spec

extra:
  recipe-maintainers:
    - mcg1969
